version: "3.9"

services:

  app:
    image: python:3.10-slim
    container_name: recsys_app
    working_dir: /app
    volumes:
      - ./app:/app
    command: bash -c "pip install fastapi uvicorn prometheus_client && uvicorn main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"

  prometheus:
    image: prom/prometheus
    container_name: prometheus
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    container_name: grafana
    ports:
      - "3000:3000"
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning
    environment:
        - GF_SMTP_ENABLED=true
        - GF_SMTP_HOST=smtp.gmail.com:587
        - GF_SMTP_USER=miraclinvinnarasi@gmail.com
        - GF_SMTP_PASSWORD=wftpntrjmgccebmp
        - GF_SMTP_FROM_ADDRESS=miraclinvinnarasi@gmail.com
        - GF_SMTP_FROM_NAME=Grafana Alerts